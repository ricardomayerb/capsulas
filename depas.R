f1 <- c(425, 430, 430, 435, 435, 435, 435, 435, 440, 440)
f2 <- c(440, 440, 440, 445, 445, 445, 445, 445, 450, 450)
f3 <- c(450, 450, 450, 450, 450, 460, 460, 460, 465, 465)
f4 <- c(465, 470, 470, 472, 475, 475, 475, 480, 480, 480)
f5 <- c(480, 485, 490, 490, 490, 500, 500, 500, 500, 510)
f6 <- c(510, 515, 525, 525, 525, 535, 549, 550, 570, 570)
f7 <- c(575, 575, 580, 590, 600, 600, 600, 600, 615, 615)

deptos <- c(f1, f2, f3, f4, f5, f6, f7)

# calcule el precio promedio de los departamentos
x_barra <- mean(deptos)
x_barra

# calcule la desviacion estándar
s <- sd(deptos)
s

# ahora calcule la version estandarizada de los precios, completando el codigo
deptos_estandarizados <- 
  
  deptos_estandarizados  

# cuales son los tres puntajes z más altos?

# cuantas observaciones están a mas de una desv est de la media?

# cuantas observaciones están a mas de 1,5 desv est de la media?

# cuantas observaciones están a mas de 2 desv est de la media?

# se cumple el teorema de Chebyshev? Explique

# use sumary apra obtener un resumen de 5 números

# use boxplot para crear un diagrama de caja